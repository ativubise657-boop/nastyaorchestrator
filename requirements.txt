fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
pydantic==2.9.0
aiofiles==24.1.0
python-dotenv==1.0.1
httpx==0.27.0
markitdown[pdf,docx,xlsx,pptx]==0.1.5
python-docx==1.1.2
beautifulsoup4==4.12.3
jinja2==3.1.4
# Только для сборки .exe (PyInstaller). На рантайме не используется,
# но безопаснее держать в общем requirements чтобы CI ставил одной командой.
pyinstaller==6.11.1

# Тесты — smoke suite в tests/, запускается в dev-gui Release workflow как gate
# и в CI перед билдом. На рантайме не используется.
pytest==9.0.3
